# Google OAuth credentials from Google Cloud Console
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# 32+ random characters used to encrypt the Gmail session cookie
SESSION_SECRET=

# Optional. Leave blank locally; set to the production callback URL on Vercel if preferred.
# Example: https://your-domain.vercel.app/api/auth/google/callback
GOOGLE_REDIRECT_URI=
